# 测试串行执行（2026-09-13）
#
# 为什么：UI 测试里有若干用例会调用 rust_i18n::set_locale（全局状态），
# 另一些用例断言具体译文（t!() 的结果）。并行执行时两者交错 ⇒ 偶发失败
# （干净 HEAD 上实测 3 次挂 1 次：model_registry::test_uv_reason_label）。
# 这不是"测试写错"，而是全局状态型测试必须串行——故在测试运行器层面固定线程数。
[env]
RUST_TEST_THREADS = "1"
