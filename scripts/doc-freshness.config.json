{
  "_comment": "check-doc-freshness.py 的入参清单（归一化白名单 / 排除名单 / 引用域 / 基准集 / slug 规范 / 生成器清单 / 拒绝域）。规格：`Zerg-内部文档/01-设计/设计-文档体系-v1.0.md`（2026-09-19 已随开发文档分家迁出工作树）§6.3 §9.1 §5.6① · `Zerg-内部文档/调研/调研-文档分块落地-门禁与迁移.md` §1 §2.3。改这个文件 = 改判据 ⇒ 必须同时改「口径」注释与说明书对应节。★ 2026-09-19 分家批6 的改动逐条见下面各 `_*_note`（零命中条目删 / 源树仍在的条目留 / `d3.spec_doc` 重指到 `../Zerg-内部文档/…`）。",
  "version": "v1.0.0",
  "corpus": [
    "docs"
  ],
  "base_roots": [
    "",
    "core",
    "agent",
    "wall",
    "ui",
    "gateway",
    "mcp",
    "shared",
    "docs",
    "scripts",
    "tools",
    "publish"
  ],
  "hard_exclude": [
    ".git",
    "node_modules",
    "target",
    "__pycache__",
    ".history",
    ".obsidian",
    "dist",
    "bin",
    "data",
    "zerg-wt",
    ".zerg",
    "vendor",
    "tools/ocr/venv"
  ],
  "mirror_paths": [],
  "_mirror_note": "★ 2026-09-19：原「docs/虫族文档」（主树镜像副本，1651 篇 20M）**已退役**（用户拍板「退」；备份 = ~/zerg-backup/docs-虫族文档-mirror-20260919.tar.gz + 解包副本 retired-docs-虫族文档-20260919/，逐文件 sha256 与原目录一致）⇒ **条目删除**（零命中条目留着 = 假覆盖）。反向探针（同日实测）：删条目前后 --scope main/wide/mirror 的语料与解析域计数**逐位一致**（main 305 篇/2027 文件 · wide 591 篇/2037 文件 · mirror=wide 591 篇/2037 文件 —— 镜像域今天本就零命中，故 `--scope mirror` ≡ `--scope wide`）。★ 镜像若日后重建（从备份解包回 docs/），请把 \"docs/虫族文档\" 加回本列，否则镜像内的引用会被当本地判红。",
  "frozen_paths": [
    "docs/项目文档/v2.0.0",
    "docs/项目文档/v2.1",
    "docs/项目文档/v2.2",
    "docs/项目文档/v2.3",
    "docs/项目文档/v2.4",
    "docs/项目文档/v2.5.0",
    "docs/项目文档/v2.5.1",
    "docs/项目文档/v2.5.2",
    "docs/项目文档/v2.5.3",
    "docs/项目文档/v2.5.4",
    "docs/项目文档/v2.5.5",
    "docs/项目文档/v2.5.6",
    "docs/项目文档/v2.5.7",
    "docs/项目文档/v2.5.8",
    "docs/项目文档/v2.5.9",
    "docs/项目文档/v2.6"
  ],
  "_frozen_note": "17 套快照里只留一套「在用版」判红（docs/项目文档/v2.5.10），另 16 套冻结归档**只登记不判红**（§8.1「保留为归档、不再新增」）；v2.6 也在冻结列（设计稿 §2.3 点名它只有 2 篇却在 AGENTS.md 的「取最大版本号」口径里会抢位，M14 未拍 ⇒ 不拿它当判据）。★ 2026-09-19 「项目文档」二次分家**批2**（**只加注、16 条目一字不动**）：这 16 条的域随**批3** 移出工作树 ⇒ 本门语料（仓内 `docs/**`）里必然**零命中** ⇒ 届时按「零命中即删」删除（死条目留着 = 假覆盖）。本批不删：对象今天仍在仓内、仍在被单列计数（`--scope wide` 现跑：冻结归档单列 16 套）。★ 同批**登记未做**：`corpus` 仍是仓内 `[\"docs\"]`——批3 后 `项目文档/` 那批文件**不再进本门语料**（D1/D2/D3 对它们的判定会静默减少）；要覆盖仓外那棵须给语料/解析域加仓外根 = 改判据，属**批3/批6** 决策，本批不动。",
  "third_party_paths": [],
  "_third_party_note": "★ 2026-09-19 分家第二步（源树从主仓删除）：`docs/调研/multi-agent-源码`（第三方源码摘录，445 件 6.2M）先 `cp -a` 到 `Zerg-内部文档/调研/multi-agent-源码/`，同日按用户令**从主仓移除**（主仓盘上已不存在，`git status` 未跟踪项归零）⇒ **零命中条目已删**（口径：零命中即删；死条目留着 = 假覆盖）。反向探针（同日 · /tmp 仿真造树）：条目在 ⇒ 第三方摘录 0 篇进语料/解析域；条目删 ⇒ 造的两个假件进扫描域（meta：扫描域 1 → 3 篇、不合规 0 → 2）⇒ 条目确在起作用；真仓侧该树不在盘上 ⇒ 删条目前后 `--scope main/wide/mirror` 的语料与命中计数**逐位一致**。源树若日后回到 `docs/调研/`，请把该条加回本列表，否则外仓 README 的引用会被当本地判红。",
  "engine_data_paths": [],
  "_engine_data_note": "★ 2026-09-19 分家：原 `[\"docs/issues\"]`（引擎数据 1086 篇）**整目录已迁至 `Zerg-内部文档/issues/`**（工作树之外）⇒ 条目**删除**（零命中：`docs/issues` 今天在盘上不存在）。反向探针（同日实测）：删条目前后 `--scope main/wide/mirror` 的语料与解析域计数**逐位一致**（该域今天本就零命中）。注意：内部任务引擎**新的**任务单落点是 `statepath.IssuesDir()`（`ZERG_ISSUES_DIR` → `<ZERG_STATE_DIR>/issues`，默认 `~/.zerg/state/issues/`），本就在语料域之外 ⇒ 本门无需为它另开一条。若日后有人把旧目录搬回 `docs/issues/`，请把 \"docs/issues\" 加回本列（否则历史报告会被当活文档判红）。",
  "fixture_paths": [
    "scripts/doc-freshness-fixtures"
  ],
  "_fixture_note": "夹具目录：自检在 $TMPDIR 造件（仓内今天为零），此条目是**声明式排除** —— 若将来有人往仓内落夹具，不许它进语料/解析域。",
  "external_domain_paths": [],
  "_external_note": "引用域开关：这些目录里的 md 引用的是**别人的仓**（源码摘录/第三方 README）。默认「扫、计数、不判红」，要判红用 --strict-all。★ 2026-09-19：① 原第三项「docs/虫族文档」（镜像副本）随镜像退役已**删除**（零命中条目）；② 原第一项「docs/02-调研/raw」随分家整目录迁至 `Zerg-内部文档/02-调研/raw` ⇒ **删除**（零命中）；③ 第二项 `docs/调研/multi-agent-源码` **随源树从主仓删除 ⇒ 已删**（零命中：该树已于 2026-09-19 按用户令移出主仓、盘上不再存在）⇒ **本列表今天为空**（该域今天零命中，删条目前后 main/wide/mirror 的语料、解析域与命中域分布计数逐位一致）。镜像/第三方树若重建，请把对应条目加回。",
  "normalize_whitelist": {
    "外链": "scheme:// · mailto: · 协议相对 // —— 分流，不入相对引用判定（外链超时见 §6.1 G-5：不阻塞）",
    "页内锚": "纯 #anchor —— 归 D3 判定，D1 只计数",
    "绝对路径样式": "以 / 或 ~ 开头 —— 站点根语义歧义，单列不判",
    "代码块内链接": "``` 围栏内的 [](…) —— 语法示例，单列不判",
    "占位符目标": "含 < > { } * $ 的目标 —— 单列不判",
    "省略号目标": "目标里含 `…` 或 `...`（路径被省略号截断）—— 单列不判",
    "语法示例目标": "无 `/`、无 `.` 且 ≤24 字的短串（如 [name](url) / [..](target)）—— 文档在讲链接语法本身 ⇒ 单列不判",
    "空目标": "[](#) 或空字符串 —— 单列不判",
    "wiki 链接": "[[…]] —— 默认单列不判（实测 13–14 处全是语法示例）",
    "大小写不符": "精确串没命中但小写命中 ⇒ 黄（APFS 判绿、Linux CI 判红）",
    "URL 掩码": "D2 抽引用前先把 URL 整段掩码（否则 host.com:80 被当裸文件名 ⇒ 假红）",
    "噪声类别（D2 必计数）": "IP:端口 · 省略号路径 · 占位符 · 命令行样式（含空格或 =）· 尾部斜杠 · 绝对路径 · URL · 无扩展名 · 非法行号",
    "易变行（D4 归一化）": "时间戳 / 版本号 / 本机路径 ⇒ 归一化后一致记黄（只有易变行不同），不一致才判红"
  },
  "d2": {
    "resolve_package_relative": true,
    "line_semantics": "单行 `file:12` · 区间 `file:10-20` · 多段 `file:1,3-5`；取其中**最大行号**与文件总行数比",
    "r2_name_pool": "去镜像、去冻结归档后的同名池（口径写死；否则镜像把每份同名都算成歧义）",
    "r2_ambiguity": ">1 份 ⇒ 黄（歧义，只入清单，不进退码）"
  },
  "d3": {
    "slug_profile": "github",
    "spec_doc": "../Zerg-内部文档/01-设计/设计-文档体系-v1.0.md",
    "include_wiki_links": false,
    "rule": "§5.6① 八条：① NFC（不做 NFKD / 不做 ASCII 折叠）② lower（非 casefold）③ 只保留 \\p{L}\\p{M}\\p{N}_- ，其余一律删除 ④ 空白→'-' 且逐字符各出一个 '-'（a  b → a--b）⑤ 空结果兜底 s<序号> ⑥ 同文件重复 slug 追加 -1/-2 ⑦ 引用一律 path#slug ⑧ 钉死版本 + 自带自测",
    "known_ambiguity": "§5.6③ 把 U+3000 列进「删除」，§5.6④ 又说空白→'-'。默认取 github（含 U+3000 → '-'，与 GitHub 逐字一致）；--slug-profile docv1.2 走字面读法。两者在本仓语料上的差异已实测（见说明书 §6 表）"
  },
  "d4": {
    "generators": [
      {
        "id": "doc-index",
        "name": "tools/doc_index_gen.py → 各版本目录 INDEX.md（生成物被手改过即 drift）",
        "cmd": [
          "python3",
          "{REPO}/tools/doc_index_gen.py",
          "{DIR}",
          "--out",
          "{OUT}"
        ],
        "mode": "out-flag",
        "cwd": "{REPO}",
        "script": "tools/doc_index_gen.py",
        "targets_glob": [
          "docs/项目文档/*/INDEX.md"
        ],
        "alt_root": "../Zerg-内部文档",
        "targets_glob_alt": [
          "项目文档/*/INDEX.md"
        ],
        "_alt_note": "★ 2026-09-19 「项目文档」二次分家**批2**（生成器靶双认 · 先仓内、缺则仓外取源根）：本生成器的产物 `docs/项目文档/*/INDEX.md` 随**批3** 移出工作树 ⇒ 仓内 glob 届时零命中。口径与 `docs/site/export-and-build.sh` 的 `ZERG_DOCS_ALT` 同根同默认（`alt_root` = `../Zerg-内部文档`，即同级 `Zerg-内部文档/`）：**仓内先匹配**；仓内**零命中**才去 `alt_root` 下按 `targets_glob_alt`（相对 `alt_root` 的路径）匹配，命中则以 `../Zerg-内部文档/…` 形态进判（生成 → $TMPDIR 重建 → 与仓外提交物逐字节比对）。**两处都零命中 ⇒ 照原口径「未声明产物 ⇒ 缺件，不给结论」rc=2**（不假绿）。两棵都在（批3 的 `cp -a` 窗口）时**只判仓内那批**（不双计）。",
        "fix": "python3 tools/doc_index_gen.py <版本目录>   # 就地重建该目录 INDEX.md（生成物不得手改）",
        "timeout_s": 60
      },
      {
        "id": "tools-md",
        "name": "tools/gen_tools_md.py → publish/docs/TOOLS.zh-CN.md（工具目录，源=工具注册表）",
        "cmd": [
          "python3",
          "{REPO}/tools/gen_tools_md.py"
        ],
        "mode": "stdout",
        "cwd": "{REPO}",
        "script": "tools/gen_tools_md.py",
        "output": "publish/docs/TOOLS.zh-CN.md",
        "inputs": [
          "core/internal/chat/chat_tool_registry.go"
        ],
        "optional": true,
        "fix": "python3 tools/gen_tools_md.py > publish/docs/TOOLS.zh-CN.md",
        "timeout_s": 60
      }
    ]
  },
  "allowlist": [
    {
      "gate": "d1",
      "file_glob": "docs/项目文档/v2.5.10/13-模块-对话chat-20260831.md",
      "target": "字不丢",
      "reason": "原文 `+[继续提问/丢弃](字不丢)`：`(字不丢)` 是「字不丢」的说明性括号，作者笔误被解析成了链接目标，不是真引用",
      "date": "2026-09-18"
    }
  ],
  "_allowlist_note": "豁免条目**必须带 reason + date**（缺一条 ⇒ 脚本 rc=2 不给结论）；只给路径的 allowlist = 无记录的洗白。★ 2026-09-19 开发文档分家批6：原 **20 条**里 **19 条**的 `file_glob` 已随分家迁出仓（`docs/01-设计/` 10 条 · `docs/02-调研/` 4 条 · `docs/调研/` 4 条 · `docs/调研/调研-文档分块落地-检索与双语.md` 1 条）⇒ 按「零命中条目留着 = 假覆盖」**删除**（删豁免只会更严：这些文件不在仓里，没有任何命中需要豁免）。**保留 1 条**：`docs/项目文档/v2.5.10/13-模块-对话chat-20260831.md`（仍在仓内、仍在命中）。剩余这条的历史沿革：2026-09-18 首跑里唯一两条登记的豁免之一（另一条 `docs/调研/调研-文档分块落地-检索与双语.md` 已随分家删除）+ 同批 D2 存量 18 条（父代批准口径，reason 前缀「历史引证·2026-09-18·父代批准口径」）——来源：D2 存量红逐条分类后**无法重映射**的活样本，细分 本稿自证实测记录（故意反例）=1、目标在本仓之外（外部仓/另一个仓）=11、目标已退役/已删=3、行内容已变·原行号处已非该内容=3；同批次**可重映射**的 61 处（路径改名/换目录 + 行号重算 + 镜像副本→主树）已直接改文档、未走豁免；每条的 target 是**精确原样 token**（不用通配）⇒ 文档里的数字一变，豁免即失效、重新变红（不构成洗白）。**该批 18 条的宿主文档全部随分家迁出 ⇒ 已随本次一并删除**。★ 2026-09-19 「项目文档」二次分家**批2**（唯一 surviving 那条：**先验证、本批不删 + 加注**）：A/B 探针（同日 · 同一扫描域）——① 临时 config（`allowlist: []`）跑 `d1` ⇒ **rc=0 与留着逐位一致**（该目标本来就被「语法示例目标」软白名单接住：d1 计数里「语法示例目标（不判）=3」）；② 但 `d1 --no-whitelist`（说明书 §4.2 表里那条「量假红」对照口径）⇒ **多出 1 条红**：`docs/项目文档/v2.5.10/13-模块-对话chat-20260831.md:352 → 字不丢`（留条目时该口径 4 条红，删条目后 6 条）—— **删它 = 宿主与引用都还在仓内时新增红** ⇒ 按任务口径**本批不删**（条目 reason/date 齐备 ⇒ 不触发 rc=2）。宿主随**批3** 移出工作树 ⇒ 该条目**届时零命中（零命中条目留着 = 假覆盖）⇒ 批3 后重评并删/改述**。",
  "refuse_domain": [
    "外链（http/https）：不判 —— 上游明写外链 can time-out a lot，Prometheus 显式忽略；要判定走 P6 的定时任务",
    "wiki 链接 [[…]]：默认不判，只单列计数（实测全是语法示例）",
    "代码块/围栏内的示例链接：不判（单列计数）—— 否则把「教别人怎么写链接」判成断链",
    "冻结归档（16 套老快照）· 镜像副本 · 第三方源码摘录 · 引擎数据（**原** docs/issues/1086 篇，已于 2026-09-19 分家至 `Zerg-内部文档/issues/`）：只登记不判（--strict-all 才判）",
    "锚在、语义变（标题文字改了但 slug 恰好不变）：机器判不了 —— 门禁只判形态（§0.2 P2）",
    "外链的 404：不判（需联网，本脚本无网络）",
    "生成物的『对不对』（内容质量）：只判『产物 == 当前生成器的输出』（产物在 ≠ 产物对，是假绿）",
    "DOCX/PDF/图片内的引用：不判（本脚本只读 md 文本）",
    "重命名/移动前的重定向表（旧 path#slug → 新 path#slug）：规范已写（T5，构建期生成），今天表不存在 ⇒ 断锚按现状报，不按重定向表报"
  ]
}
