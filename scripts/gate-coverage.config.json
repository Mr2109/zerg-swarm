{
  "_comment": "check-gate-coverage.py 的入参清单（扫描面 / 硬排除 / 白名单 / 基线棘轮）。规格：docs/项目文档/v2.5.10/债务台账-20260918.md §0（哪些域在所有门之外）与 §7 建议门①。★ 改这个文件 = 改判据 ⇒ 必须同时改 scripts/check-gate-coverage.md 的「口径」节。★ 只许改 baseline 那一个数字往下调（永不上调）。",
  "schema": "gate-coverage/1",

  "baseline": 69,
  "_baseline_note": "2026-09-18 首测（本门新建当天 · 22:4x）：scripts/*.sh 32 + scripts/*.py 53 + 无后缀 2 = 87 条候选，其中「闸文件里查无此名」= 74 条，白名单豁免 0 ⇒ 棘轮命中数 = 74 = 本行 baseline ⇒ 首跑 rc=0（但仍打印「剩余 74 条」，不许悄悄绿）。★ 与《债务台账-20260918》§0/§6 的 71/82 口径差 +3：台账现跑时点（22:2x）之后，同一时点有并发会话新建了 scripts/check-gate-coverage.py（本门自身）· scripts/check-version-sources.py（门②）· scripts/check-wired-scripts.py（门③）三条脚本。★ 第二波（2026-09-18 22:5x）把三只新门挂进 precommit-gates.sh 的 `gates` scope 步骤表（`tri`/`tri`/`tri-report`）：这 3 条由 ③ 落到 ①；**另有 2 条也离开 ③**——`scripts/start-zerg-core.sh` 与 `scripts/start-zerg-ui.sh` 只因新增的「四棵外围目录 + 顶层入口脚本语法」步**点到了它们的同名兄弟**（顶层 `./start-zerg-core.sh` / `./start-zerg-ui.sh`）而被判「被提到」（② 弱挂载档 · 同一口径下的 basename 命中，**不是真接线**）。⇒ 复测 ③ = **74 − 3 − 2 = 69**，tier ② 8 → 10，三档之和仍 = 87 候选 ✓ ⇒ 按下调号令把 baseline 由 74 改成 **69**（只改这一个数字）。★ **只许下调、永不上调**：上调 = 放宽口径 ⇒ 必须走拍板。",

  "scan": {
    "build_manifests": ["go.mod", "Cargo.toml"],
    "script_globs": ["scripts/*.sh", "scripts/*.py"],
    "nosuffix_dir": "scripts"
  },
  "_scan_note": "build_manifests = 断言 A 的「这是棵构建树」判据；script_globs + nosuffix_dir = 断言 B 的候选集（无后缀件要「有执行位或有 shebang」才算脚本）。",

  "exclude": {
    "path_components": [
      ".git", "vendor", "node_modules", "target", "__pycache__", ".history", ".obsidian",
      "dist", "bin", "data", "zerg-wt", ".zerg", "venv", ".cargo", ".venv", "htmlcov"
    ],
    "prefixes": ["tools/ocr/venv"]
  },
  "_exclude_note": "第一档硬排除（结构级：不是「放过」，是压根不进候选集）。口径逐字对齐 check-doc-name/meta/freshness 的 EXCL/PRE 名单 + 债务台账 §0 第 9 行的 13 个硬排除名。★ 这里多出来的 `.cargo`/`.venv`/`htmlcov` 是构建/虚拟环境目录，与既有三只门同源。",

  "whitelist": {
    "build_dirs": [
      {
        "path": "docs/调研/子报告-内建调试版-20260917/redaction-probe",
        "reason": "调研子报告内的一次性复现夹具：一个独立的 Go module（main.go/redact.go/verify.go/fuzz_test.go + 它自己的 report.md），供内建调试版调研时复现 redact 探针用；它不属于本仓构建面，任何 add_step 都不该把它编进产物。★ 不是「为了首跑绿」——本条在报告里计入「② 白名单豁免（第二档）」逐条可见，且带日期；若日后它变成活目录，请删掉本条让它真红。",
        "date": "2026-09-18"
      },
      {
        "path": "docs/虫族文档/调研/子报告-内建调试版-20260917/redaction-probe",
        "reason": "上一行那条的**主树镜像副本**（docs/虫族文档/ 整体是 docs/ 的镜像，设计稿 §2.7）：镜像内条目按主树同名条目的同一理由处理，不另判。",
        "date": "2026-09-18"
      }
    ],
    "scripts": []
  },
  "_whitelist_note": "第二档白名单：每条必须给 reason + date（缺任一 ⇒ rc=2，不是红 —— 写不清理由的豁免就是洗白）。scripts 一栏今天**故意留空**：74 条里没有一条具备「永远不该进步骤表」的理由 —— 存量债一律走第三档基线棘轮，逐批接线后下调 baseline，不许用白名单把真漏项洗干净。"
}
