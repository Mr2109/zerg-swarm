=== 环境 ===
macOS：26.5.1（arm64）
Python：3.9.6
茧壁：wall/target/debug/zerg-wall ⇒ zerg-wall 0.1.0

=== ⓪ 制品可复现自检（CARGO_INCREMENTAL=0 连编两次，中间 touch 强制重编）===
  为什么要它：回执里记了制品 sha，而**默认（增量编译）下同一份源码连编两次 sha 不同**（本机实测）
  ⇒ 先把构建钉成可复现的，回执里那个 sha 才不是一个编号。
  build#1：d1cfa001c6217432e5685b3cb0c1018d2343ad45183d4a28e5022afaf8c18816
  build#2：d1cfa001c6217432e5685b3cb0c1018d2343ad45183d4a28e5022afaf8c18816
  两次**逐字节相同**（可复现构建）

=== 制品与源码 sha256（哪一版门禁跑出来的）===
  8e739a4eb7e683491a8dbab918ffc79a96694f4f8dc44ff7d71051a4b6059f4d  scripts/sandbox-probes/verify-two-states.py
  5fc0be0299a72da2623ef6910b498d9a9d99eeef5b51a8a09bceedd68274cf95  scripts/sandbox-probes/negctl-two-states.sh
  1d3ded90f390449e845fec2e65a5ec387c8e073df4032e2a79dab7f8fb1a52f3  scripts/sandbox-probes/miniprobe.py
  0669c7c7838c041fe8424e565922ee285071a586a0e05c4c6b97b15e90bdb71f  scripts/sandbox-probes/conn_jit.py
  d7566516ec638f23a81f0a529a70cc7a9354fca778895be13b7c77ef8df354a1  scripts/sandbox-probes/lx2-linux.py
  b0d6c4efb246decdb8ae6842bed784dad6bbb3a7e67cf2e9790beb7cdb55eec8  scripts/sandbox-probes/pF.sb
  eb36e13df4f415ecfcbbea6880f23c649ca149e843ea08f7a59436355f777ce3  scripts/two-states-gate-mutate.py
  660a4c4a148469dd78fca8a0cf55dcdc4dd3709e59e7b1ac1b7532d9fc553238  scripts/two-states-wall-evidence.py
  d1cfa001c6217432e5685b3cb0c1018d2343ad45183d4a28e5022afaf8c18816  wall/target/debug/zerg-wall
  44cba4e3dcb2fa4429e69f73dfdffbaea38378aef6000372cdab758a8442e725  wall/src/platform/macos.rs
  18c42cae7ee7d60c2e605fcb657138620149a319721d1e51611617b5a42582ec  wall/src/spec.rs

生成时的 HEAD（本批的父提交；回执与它证据的改动同批提交）：ecc1f410dacb6fad52b1c071a9c6f9ea6e73f5ba
判据（可复跑）：python3 scripts/two-states-wall-evidence.py
  ① python3 scripts/sandbox-probes/verify-two-states.py
  ② python3 scripts/sandbox-probes/verify-two-states.py --wall wall/target/debug/zerg-wall
  ③ python3 scripts/sandbox-probes/verify-two-states.py --self-test --wall wall/target/debug/zerg-wall
  ④ bash scripts/sandbox-probes/negctl-two-states.sh
  ⑤ python3 scripts/two-states-gate-mutate.py

=== ① 默认路线（行为与历史一致） ===
$ python3 scripts/sandbox-probes/verify-two-states.py
  茧壁 · 判据 7 两态可验（平台 darwin · 原生 sandbox-exec）
    判别目标                       1.1.1.1:443 基线实测 OPEN ⇒ 可达（有区分度）
    断言 A 空间内通信可用               PASS     unix socket bind=ok · loopback 自己连自己=ok
    断言 B 出网被拦                  PASS     基线可达（OPEN）· 封闭态被拦（BLOCKED:PermissionError）
    （信息）JIT W^X                ok:42    §10.10：基线/封闭态都应 ok:42（判据 7 不含它）
rc=0（期望 0）

=== ② 茧壁路线 + 两路对拍 ===
$ python3 scripts/sandbox-probes/verify-two-states.py --wall wall/target/debug/zerg-wall
  茧壁 · 判据 7 两态可验（平台 darwin · 原生 sandbox-exec + 茧壁 wall/target/debug/zerg-wall）
    判别目标                       1.1.1.1:443 基线实测 OPEN ⇒ 可达（有区分度）
    断言 A 空间内通信可用               PASS     unix socket bind=ok · loopback 自己连自己=ok
    断言 B 出网被拦                  PASS     基线可达（OPEN）· 封闭态被拦（BLOCKED:PermissionError）
    （信息）JIT W^X                ok:42    §10.10：基线/封闭态都应 ok:42（判据 7 不含它）
    茧壁断言 A（信息）                 PASS     unix socket bind=ok · loopback 自己连自己=ok
    茧壁断言 B（信息）                 PASS     基线可达（OPEN）· 封闭态被拦（BLOCKED:PermissionError）
    两路对拍（茧壁 vs 原生黄金配方）         一致       两路逐项一致（A=True · B=True · JIT=ok:42）
rc=0（期望 0）

=== ③ 自检（护栏 + 四态成对 + 路线自检） ===
$ python3 scripts/sandbox-probes/verify-two-states.py --self-test --wall wall/target/debug/zerg-wall
    ① 护栏自检（合成输入）
      [ok] classify: OPEN ⇒ 可达
      [ok] classify: ConnectionRefused（裸词）⇒ 可达
      [ok] classify: BLOCKED:ConnectionRefused ⇒ 可达（对方回了 RST）
      [ok] classify: BLOCKED:PermissionError ⇒ 被拦
      [ok] classify: 空串 ⇒ 被拦（不知道不许当做到）
      [ok] 断言 A 能红：unix/loopback 被拒 ⇒ FAIL
      [ok] 断言 A 能绿：unix/loopback 都 ok ⇒ PASS
      [ok] 断言 B 能红：封闭态仍可达 ⇒ FAIL
      [ok] 断言 B 能绿：基线可达 + 封闭态被拦 ⇒ PASS
      [ok] 断言 B 无区分度：基线也不可达 ⇒ 既非红也非绿（rc=2 口径）
      [ok] 探针缺失必须硬失败
    ② 四态成对（活体变异：把封闭件换成不该成立的形态，断言必须变红）
       判别目标 1.1.1.1:443（基线 OPEN）
      [ok] 基线（无封闭）                          A=True(期望True) B=False(期望False) JIT=ok:42(期望ok:42) —— B 的红版本：出网本就可达
      [ok] pF 黄金配方                          A=True(期望True) B=True(期望True) JIT=ok:42(期望ok:42) —— 两态都该成立
      [ok] pA 无网络规则                         A=False(期望False) B=True(期望True) JIT=ok:42(期望(不约束)) —— A 的红版本：unix/loopback 被拒
      [ok] pE 全放网络 network*                 A=True(期望True) B=False(期望False) JIT=ok:42(期望(不约束)) —— B 的红版本：封闭态出网仍开
      [ok] 茧壁（--wall，与原生对拍）                 A=True B=True JIT=ok:42 —— 两路逐项一致（A=True · B=True · JIT=ok:42）
    ③ 路线自检（--wall）
       现状（信息，非断言）：Linux 配方的 argv 没有 `--unshare-net` —— 缺它就是出网仍开，这是**已知缺口**（现行生产配方同形，待拍板第 8 条）
      [ok] ③-a 茧壁拒绝配方时路线必须硬失败（不许当「已封闭」）
      [ok] ③-b 生成的 Linux 配方茧壁必须接受（且 argv[0]=bwrap）
    ✓ 四态成对全部与文档口径一致：断言 A 与断言 B **各自都有红的版本**（判据 7 的「两态可验」）
rc=0（期望 0）

=== ④ 负例活体控制（四条都必须硬失败） ===
$ bash scripts/sandbox-probes/negctl-two-states.sh
  === 负例① 探针缺失（pF.sb 临时挪走）===
  ✗ 硬失败（rc=2，不是跳过）：探针缺失或路径漂移：pF.sb（在 scripts/sandbox-probes 下）⇒ 判据 7 要求硬失败，不许静默跳过
  负例① rc=2 （期望 2）
  还原 sha 比对：before=b0d6c4efb246decdb8ae6842bed784dad6bbb3a7e67cf2e9790beb7cdb55eec8 after=b0d6c4efb246decdb8ae6842bed784dad6bbb3a7e67cf2e9790beb7cdb55eec8
  
  === 负例② 基线无区分度（指定一个不可达的判别目标）===
  ✗ 硬失败（rc=2，不是跳过）：候选判别目标全都基线不可达 ⇒ 出网探针无区分度（<worker-ip>:9→BLOCKED:timeout）⇒ 用 TWO_STATE_TARGETS 指定一个本机可达的局域网目标再跑
  负例② rc=2 （期望 2）
  
  === 负例③（--wall）路径漂移：茧壁二进制不存在 ⇒ 必须 rc=2 ===
  ✗ 硬失败（rc=2，不是跳过）：茧壁二进制不在 /private/tmp/没有这个茧壁-zerg-wall ⇒ 先 `cd wall && cargo build`（门禁不静默跳过，也不替人编）
  负例③ rc=2 （期望 2）
  
  === 负例④ 参数拼错（--wal）⇒ 必须 rc=2（认不得的参数一律拒，不许跑成另一条路线）===
  ✗ 硬失败（rc=2，不是跳过）：认不得的参数 '--wal' ——拒（不许静默忽略；用法见 --help）
  负例④ rc=2 （期望 2）
  
  === 还原确认（该件应无改动）===
   M scripts/sandbox-probes/negctl-two-states.sh
   M scripts/sandbox-probes/verify-two-states.py
  ?? scripts/sandbox-probes/evidence-two-states-wall-20260916.txt
  全部负例按期望硬失败 ✓
rc=0（期望 0）

=== ⑤ 门禁变异验证（三条都必须红 ⇒ 还原 ⇒ 复跑绿） ===
$ python3 scripts/two-states-gate-mutate.py
  === 判据 7 门禁变异验证（目标 scripts/sandbox-probes/verify-two-states.py · 门禁 = --self-test --wall）===
  基线：门禁 rc=0（期望 0）
  ✓ [G1] 茧壁路线静默回落成原生黄金配方（探针照样输出、断言照样全过） ⇒ 门禁 rc=2（期望非 0）
       红的原文：[ok] 探针缺失必须硬失败
       红的原文：✗ 硬失败（rc=2，不是跳过）：茧壁路线没有茧壁自己的留痕（缺 '茧壁计划（run）：'、'茧壁策略（本进程直调 Seatbelt'、'(deny default)'）⇒ 这条路线**没有真的经过茧壁** ⇒ 硬失败
  ✓ [G1] 还原后 sha 逐字节一致
  ✓ [G1] 复跑：rc=0（期望 0）
  ✓ [G2] 路线起不来时当成「全被拦」（起不来的沙箱看起来最安全） ⇒ 门禁 rc=1（期望非 0）
       红的原文：[ok] 探针缺失必须硬失败
       红的原文：[✗] ③-a 茧壁拒绝配方时路线必须硬失败（不许当「已封闭」）
       红的原文：✗ 路线自检不过：['③-a 茧壁拒绝配方时路线必须硬失败（不许当「已封闭」）']
  ✓ [G2] 还原后 sha 逐字节一致
  ✓ [G2] 复跑：rc=0（期望 0）
  ✓ [G3] 断言 B 不再判「封闭态出网仍可达」（红的那一版被抹掉） ⇒ 门禁 rc=1（期望非 0）
       红的原文：[✗] 断言 B 能红：封闭态仍可达 ⇒ FAIL
       红的原文：[ok] 探针缺失必须硬失败
       红的原文：✗ 护栏自检未过：['断言 B 能红：封闭态仍可达 ⇒ FAIL'] ⇒ 本脚本自身的判定层不可信，先修它
  ✓ [G3] 还原后 sha 逐字节一致
  ✓ [G3] 复跑：rc=0（期望 0）
  
    G1 茧壁路线静默回落成原生黄金配方（探针照样输出、断言照样全过） · 变异前 sha=8e739a4eb7e6…
    G2 路线起不来时当成「全被拦」（起不来的沙箱看起来最安全） · 变异前 sha=8e739a4eb7e6…
    G3 断言 B 不再判「封闭态出网仍可达」（红的那一版被抹掉） · 变异前 sha=8e739a4eb7e6…
  变异验证全绿：每条都「红 ⇒ 还原 sha 一致 ⇒ 复跑绿」
rc=0（期望 0）

=== 断言汇总（9 条，红 0 条）===
  PASS ⓪ 制品可复现（CARGO_INCREMENTAL=0 连编两次逐字节相同）  —— d1cfa001c6217432…
  PASS ① 默认路线（行为与历史一致）  —— rc=0
  PASS ② 茧壁路线 + 两路对拍  —— rc=0
  PASS ③ 自检（护栏 + 四态成对 + 路线自检）  —— rc=0
  PASS ④ 负例活体控制（四条都必须硬失败）  —— rc=0
  PASS ⑤ 门禁变异验证（三条都必须红 ⇒ 还原 ⇒ 复跑绿）  —— rc=0
  PASS ④ 内容断言：四条负例逐条 rc=2（不是「整体不为 0」）  —— 四条齐
  PASS ⑤ 内容断言：G1/G2/G3 三条变异都被记录（各自红在不同判据）  —— 三条齐
  PASS ② 内容断言：两路对拍=一致 · 茧壁断言 A/B 都 PASS  —— PASS 出现 4 次
